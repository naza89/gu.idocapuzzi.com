# Dependencies
node_modules/

# Next.js
.next/
out/

# Environment variables (NEVER commit secrets)
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel

# Claude Code (worktrees y estado interno — no commitear)
.claude/worktrees/
# settings.local.json = estado local + permisos (contuvo credenciales en claro). NUNCA commitear.
.claude/settings.local.json

# Snapshot de trabajo anterior (referencia local, no subir)
.antigravity/

# Artefactos de test de webhooks OCA (marcados NO COMMITEAR en su header)
oca-fire-step.mjs
oca-webhook-sim.mjs

# Originales del shoot: bandeja de entrada local. Al repo entran sólo los WebP
# generados en public/assets/images/products/ (ver docs/internal/FOTOS_PRODUCTO_NOMBRES.md).
public/assets/images/fotoproducto/

# TypeScript
*.tsbuildinfo
next-env.d.ts

.vercel

# Masters de video y gráficas del shoot: fuentes pesadas, quedan locales.
# Al repo entran sólo los derivados optimizados (mp4/webm y webp).
public/assets/video/*.mov
public/assets/images/gráficas/
